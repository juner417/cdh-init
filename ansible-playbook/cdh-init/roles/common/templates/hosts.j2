# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

# cdh Manager

# cdh APP

# cdh cluster - ansible generation start
{% for item in hostvars %}
{{ hostvars[item]['ansible_ssh_host'] }} {% if hostvars[item]['ansible_domain'] is defined %}  {{ hostvars[item]['ansible_domain'] }}  {% endif %}  {{ item }} 
{% endfor %}
# cdh cluster - ansible generation end
